# 2026-09-22 菜单暂停版（公开版 0.1.0）

公开版 0.1.0 沿用开发版 0.1.5-diag 位流，Diagnostic version 仍为 5；下方开发版本号保留作排查参考。

上一版六款原始 ROM 已获用户确认：实机画面、声音、按键正常。本版补上 Pocket 菜单自动暂停，显示模式和 ROM 补偿不变。

2026-09-22 后续实机反馈（Pocket 固件 2.7）：打开/关闭菜单可以正常暂停/恢复游戏和声音。以下步骤保留用于回归及问题排查，不表示所有边界场景均已实机覆盖。

## 本次上机步骤

1. 备份存档与设置，退出核心后安装完整 0.1.5 包，位流和 JSON 均需更新。目录为 `Cores/maxhoov.VirtualBoy`。
2. 使用原始 ROM，确认 Diagnostic version = `5 - Menu pause`，Output test = Off。
3. 进入有动画、计时或音乐的游戏场景，呼出 Pocket 菜单。菜单停留期间游戏应暂停且静音；安全排空后 CPU activity 应为 `5 - Menu paused`。若曾出现非法指令，仍优先显示 4。
4. 停留至少 10 秒后关闭菜单，确认游戏从原处继续，音乐恢复，没有跳过游戏时间、重启或卡死。对六款游戏重复检查，并尝试快速开关菜单。
5. 关闭菜单时故意按住游戏按键，应不会直接传入游戏；松开再按后应恢复正常。菜单内修改显示模式后退出也应正常。
6. 如异常，请报告 ROM 名、菜单停留时间、CPU activity，以及是否恢复画面/声音/按键。

暂停时画面扫描和 SDRAM 刷新继续，不关停物理时钟；静音通过音频数据通道完成，I2S 时钟继续。此功能不是即时存档或休眠支持。菜单内 VIP 仍可能报告有扫描活动，VSU 监测的是原始样本，不等于最终已静音的输出。由于观察菜单本身会触发暂停，CPU 5 是预期状态，不能再要求菜单内显示 CPU 2。

## 0.1.4 ROM 顺序补偿与元数据更新（历史）

元数据/图片更新包：作者改为 maxhoov、分类固定 Console，并加入 Virtual Boy 平台图；位流未改变。升级时先备份存档与设置，把旧 `Cores/PocketVB.VirtualBoy` 重命名为 `Cores/maxhoov.VirtualBoy` 后再覆盖新包，不要保留两个核心目录。Diagnostic version 仍为 4。

用户已确认：在版本 3 上使用 Mario Clash 半字交换诊断副本，有游戏画面、声音，CPU activity = 2。版本 4 把等效变换移入 ROM 加载器，存档不受此变换影响。首次非法指令捕获仍保留。新版原始 ROM 实机效果尚待复测。

## 本次上机步骤

1. 备份存档，完全退出核心，将完整 0.1.4 包合并到 SD 卡根目录，同时替换位流与 JSON。
2. **换回原始 ROM。不要加载 `Mario Clash - halfword probe.vb` 或其他经过半字交换的副本，否则会重复补偿。**
3. 确认 Diagnostic version 为 `4 - ROM order fix`，Output test 为 Off。
4. 先测试原始 Mario Clash，再测试 Wario Land、Mario's Tennis、Panic Bomber。分别报告能否进入游戏、有无画面/声音、按键是否正常，以及 CPU activity。
5. 若 CPU activity = 4，打开 Output test 并关闭菜单，拍下首次故障的两行数字，同时报告具体 ROM 文件名。若仍黑屏但 CPU 不为 4，记录 ROM load status、Boot stage、CPU/VIP/VSU activity。

CPU 2 代表最近窗口有指令提交，不等于全面兼容。CPU 3 可能是正常等待中断；CPU 4 为非法指令历史。音频、显示模式、长时间运行及存档持久化仍需进一步验证。

## 0.1.3 首次非法指令诊断（历史）

### 当时需要反馈：测试画面照片

0.1.2 实机已确认 CPU activity = 4、VIP activity = 2、VSU activity = 0，独立棋盘格与测试音正常。基本输出路径可用，下一步需要确定 CPU 第一次出错时的地址和指令内容。

1. 备份存档，退出核心，安装完整 0.1.3 包，同时替换位流和配置。
2. 先用 Mario Clash，确认 Diagnostic version 为 `3 - Fault capture`。
3. 等待数秒并记录 CPU/VIP/VSU activity；若 CPU 为 4，降低音量，打开 Output test 并关闭菜单。若游戏已有画面或 CPU 不再为 4，直接报告实际表现和状态，不必等待它出错。
4. 棋盘格左上角会出现黑底两行白色十六进制数字。请拍照，并说明 ROM 文件名：第一行是首次非法指令 PC（8 位），第二行是 CPU 指令字 IW（8 位）。
5. 第二行高 16 位为第一个指令半字，低 16 位为随后的半字；后者不一定属于该非法指令。不要按普通 32 位小端数直接比较整行。

记录在第一次非法指令时锁存，之后的异常不会覆盖；重新加载 ROM 或复位会清除。无非法指令时不会出现数字面板。显示采用七段字形，B/D 显示为 b/d，拍照比手动抄写更可靠。

本地 Mario Clash 与 Panic Bomber 的前 10 ms 启动仿真未复现非法指令；Mario Clash 随后延长到 50 ms，执行 170,042 条指令并进入初始化代码，仍未触发非法指令。这不验证完整 ROM 下载或实际板级内存时序，也不表示游戏已可运行；故仍需首次硬件故障数据。

## 0.1.2 操作及状态说明（历史）

上次实机反馈：ROM load status = 0、Boot stage = 10、Diagnostic version = 1。这证明没有检测到下载队列溢出，且 CPU 至少提交过一条指令；尚不能证明 ROM 内容正确、CPU 持续执行或音视频正常。

本次增加约 210 ms 窗口的运行监测，以及绕过游戏的独立测试画面和低幅度测试音。仍是诊断包，不是已修复黑屏的发行版。

## 本次上机步骤

1. 备份存档，完全退出核心，将完整安装包合并到 SD 卡根目录，同时替换位流和 JSON。
2. 加载此前黑屏的游戏，等待至少 3 秒。确认 Diagnostic version 为 `2 - Diag 20260920`。
3. 拍下 ROM load status、Boot stage、CPU activity、VIP activity、VSU activity。不要手动更改这些只读诊断列表。
4. 先降低掌机音量，将 Output test 改为 `On - Pattern + tones`，关闭菜单并等待 2 秒。应看到带白色边框的灰阶棋盘格，听到左右声道不同音高的连续测试音。
5. 报告测试画面和测试音是否分别出现。再将 Output test 设为 Off，恢复游戏输出。

| 状态 | 含义 |
|---|---|
| CPU 0 / 1 | 复位中 / 最近窗口没有指令提交 |
| CPU 2 | 最近窗口有指令提交；不证明游戏逻辑正确 |
| CPU 3 | CPU 当前处于 HALT，可能在等待中断 |
| CPU 4 | 曾检测到非法指令，复位前保持 |
| VIP 0 / 1 | 复位中 / 最近窗口没有帧同步 |
| VIP 2 / 3 | 有帧同步但可见区全黑 / 观察到非黑像素 |
| VSU 0 / 1 | 最近窗口样本全零 / 存在非零样本，不保证可听声音 |

测试模式不停止游戏，活动状态仍监测原始游戏信号。切换时允许有短暂过渡。测试模式不依赖 ROM、CPU 或 VIP 内存，但仍使用同一输出适配器、时钟和 Pocket 接口。若测试画面和声音都正常，下一步重点查游戏运行和内存数据；若任一仍不正常，继续查对应输出链路。

## 上一版诊断及 Boot stage 定义

本版修复桥接总线读回值未保持的问题，并增加文字启动状态。它用于定位四款 ROM 均黑屏的问题，不承诺已经修复游戏启动。

先备份存档，完全退出核心，再把安装包内目录合并到 SD 卡根目录，替换核心位流及 JSON。仅替换旧的菜单补丁不够。

加载一款此前黑屏的游戏，等待数秒后打开核心菜单。请不要手动切换诊断选项，拍下以下三项：

- Diagnostic version：应为 `1 - Diag 20260917`。显示 `0 - Old bitstream` 时先检查是否复制了完整的新包。
- ROM load status (diag)：溢出标志。
- Boot stage：当前启动阶段。

| Boot stage | 含义 |
|---|---|
| 0 | PLL 尚未锁定 |
| 1 | 未收到 ROM 数据槽写入请求 |
| 2 | 下载尚未结束，含等待完成通知/队列排空 |
| 3 | ROM 存储服务尚未就绪 |
| 4 | 未收到/同步到主机复位释放 |
| 5 | 工作内存及 VIP 内存正在清零 |
| 6 | 控制台仍被保持在复位状态 |
| 7 | 已释放复位，但尚未观察到 ROM 读取请求 |
| 8 | 观察到 ROM 请求，但尚未得到 ROM 响应 |
| 9 | ROM 已响应，尚未观察到指令提交 |
| 10 | 至少有一条指令提交；不代表游戏正常运行或显示正常 |
| 11 | 下载 FIFO 溢出，禁止启动 |

这些字段只用于观察，RTL 忽略对诊断地址的写入。跨时钟同步可能在阶段切换时产生很短的中间值，应以等待数秒后的稳定值为准。

已确认的读回问题：APF 在发出下一次读取脉冲前发送上一次读取的结果；旧顶层却用正在变化的地址选择返回值。新测试在旧代码上报告 `bridge response not held until next read`，修复后通过。该缺陷意味着旧菜单状态不应被当成完整启动链路的证明。

协议行为可从模板 `apf/io_bridge_peripheral.v` 的 ST_READ_0/ST_READ_1 和官方 [Bus Communication](https://www.analogue.co/developer/docs/bus-communication) 核对。
