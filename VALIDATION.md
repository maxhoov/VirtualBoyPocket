# 移植验证记录

更新：2026-09-22。此文件记录实际执行结果，不等同于硬件认证。

公开发行版本按用户要求重置为 0.1.0，不带 -diag 后缀。沿用开发版 0.1.5-diag 的已测位流，硬件 Diagnostic version 保持 5；以下版本号作为历史验证记录保留。

## 2026-09-22 后续实机确认与公开发行准备

用户确认：打开/关闭菜单可以正常暂停/恢复游戏和声音，测试环境为 Pocket 本体、固件 2.7。此反馈补充并取代下方历史过程中的“暂停待实机验证”状态；不扩大为六款游戏所有暂停边界场景均已覆盖，也不代表 Dock、休眠或存档持久化已验证。

公开发行准备保留版本 0.1.5-diag，填写项目 URL 并更新说明，不修改 RTL 或重新编译位流。

## 2026-09-22 六款实机反馈与菜单暂停（0.1.5-diag）

用户确认 rom/ 目录六款原始游戏（Mario Clash、Mario's Tennis、Panic Bomber、SD Gundam - Dimension War、Tobidase! Panibon、Virtual Boy Wario Land）的实机画面、声音和按键均正常；同时反馈 Pocket 菜单未自动暂停。代码确认 osnotify_inmenu 已收到但未消费，savestate_pause_req_i 原先恒为零。

0.1.5 接入菜单同步、安全暂停、保持扫描、音频静音及按键释放保护，诊断版本为 5。最初回归发现 menu_paused 在过程块使用前尚未声明，调整声明位置后重跑；同步改为已完成的暂停握手输出，而非依赖持续扫描时的瞬时 VIP 排空状态。首轮编译已中止，后续结果须来自修正后的完整编译。具体测试/编译结果随后记录；尚无本版实机暂停反馈。

八组回归通过。顶层原创 ROM 在 HALT 状态经真实 APF 00B0 通知暂停 25 ms，PC 保持 FFFFFFFE，期间仍产生一帧输出同步；另一次短暂停也通过。检查菜单静音后的实际 I2S 数据线、非零样本恢复、保持按键不透传、松开重新生效及只读 CPU 状态 5。后续综合异常退出并标记失败（未报告明确 HDL 错误），未发布该结果；构建缓存已移到 output/build-cache-0.1.5-interrupted-20260922 保留，重新开始干净构建。

原始 Mario Clash 启动 50 ms：170,042 条指令、188,733 个 ROM 响应采样、PC FFFFD7D8，无非法指令，与旧版启动基线一致。随后两次菜单暂停/恢复通过，首次暂停 PC FFFFD7DA，25 ms 内继续产生一帧同步；中途取消未完成的 VIP 排空请求也成功恢复。原始 Wario Land 启动 10 ms：38,859 条指令、41,046 个响应采样、PC FFFBE96E，与旧版一致；同样通过两次菜单暂停及取消请求检查。日志为 tests/game_v5_mario_menu.log、tests/game_v5_wario_menu.log。这些是启动阶段测试，不代表已模拟完整游戏场景。

干净编译于 09:30 完成，资源 17,148 / 18,480 ALM（93%）、294 M10K、5 DSP；但 setup 最差 -0.718 ns，打包脚本拒绝发布。详细路径为 VIP H-bias 调度器 row_screen_start_q 到内部 DRAM RAM 块写使能的长组合路径。随后仅调整构建优化：H-bias 引擎 SPEED、FITTER_EFFORT 为 STANDARD FIT，保留原时钟与全部时序例外，不修改游戏 RTL 行为。失败日志与路径保存在 output/build-0.1.5-timing-failed-20260922.log 和 output/critical-paths-0.1.5-failed-20260922.rpt。最终结果另记。

速度优化构建仍有 -0.470 ns setup（17,171 ALM），未发布，日志保留为 output/build-0.1.5-speed-timing-failed-20260922.log。继续优化同一 H-bias 路径：将 MY + (screen - GY) 重排为 (MY - GY) + screen，让偏移与屏幕行计算并行。保留原先 9 位 screen 回绕、local_y 输出、所有状态机及周期，不增加流水级。所有中间值在原有有符号范围内，新增 18 位偏移线 keep 避免综合重新合并成长串联链。

九组回归通过：新增 462,144 组实际 H-bias 调度器运算对比（所有 GY、MY 边界、屏幕回绕及确定性随机组合）；菜单内复位并恢复复位向量程序通过；暂停期间 SDRAM 刷新和扫描继续。复位后诊断值需跨时钟返回，测试补足 20 个桥接周期后再读取，不改变硬件延时。最终时序结果随后记录。

最终构建于 2026-09-22 10:10 完成，0 errors、252 warnings；现有约束下最差 setup +0.221 ns、hold +0.110 ns，所有摘要余量非负。资源 17,143 / 18,480 ALM（93%）、294 / 308 M10K（95%）、5 / 66 DSP。保持原时钟及全部时序例外，未放宽约束。部分模板外部接口仍未完整约束，不能视为整板认证。

运算优化后的 Panic Bomber 启动 10 ms 为 38,858 条指令、41,045 个 ROM 响应采样、PC 0700002A，无非法指令；随后两次暂停、25 ms 持续扫描/刷新、I2S 静音/恢复、按键释放保护、取消未完成暂停均通过（tests/game_v5_panic_menu.log）。RBF_R 长度 2,157,944 字节，SHA-256：`F361363F64C72E136DAFF1970F6B6FECED4ACD52931BB5EBF58B2BEB393436FD`。菜单暂停实际掌机表现仍待用户反馈，不能用上一版六款运行成功代替本版验证。

## 元数据与平台图更新（同一 0.1.4 位流）

按用户要求，category 固定为 Console、author 改为 maxhoov，安装目录同步改为 Cores/maxhoov.VirtualBoy。替换原先与模板 ex_platform.bin 完全相同的占位 virtualboy.bin，改用本项目原创 SVG 插图生成的 521×165 灰阶图，编码后 171,930 字节。图像按逆时针 90°、亮度高字节/低字节零顺序存储，生成工具逐像素回读验证，并检查解码后的 PNG 预览。打包只加入 virtualboy.bin，不带模板图、SVG 或预览 PNG。不需要重新综合；图像在 Pocket 菜单中的实际显示及更名后设置继承仍待实机确认。

## 2026-09-21 半字补偿实机对照与 0.1.4-diag

用户在版本 3 加载半字交换诊断副本后反馈“有画面、有声音、CPU activity = 2”。按本轮对照步骤为 Mario Clash；尚未收到四款原始 ROM 的新版测试结果。该结果确认输入半字交换能够补偿已观察到的故障，不足以区分底层加载布局或读突发选择的原因。

版本 4 仅在 ROM FIFO 输入将 file_word 的两个 16 位半字交换，等价于旧核心加载诊断副本；存档仍直接使用原有 file_word。原始 ROM 无需修改，已交换副本不能继续使用。诊断标记为 4，保留首次非法指令和输出测试。

顶层仿真默认建模实机观察到的 XOR 2 读地址效应，明确不将其当成经过验证的芯片时序机制。原创 512 字节程序全部经桥接下载，逐字检查实际存入模型的交换布局，并检查存档导入/导出字节顺序。游戏预加载模型也应用加载器变换，最后一字仍走真实桥接；每个 ROM 响应都与原始文件比较，不再跳过比对。独立 tb_sdram 仍保持理想芯片合同测试。`-IdealReadOrder` 是新版负向测试，应检测出重复补偿/不匹配，不代表正常工作模式。

本版八组回归已通过，包含完整 512 字节桥接下载后的半字布局、ROM 复位向量执行、存档导入/导出顺序及诊断版本 4。新增布局检查最初放在单字提交后，早于 SDRAM 初始化/队列排空，导致测试过早读未写入内存；移至下载排空、复位释放之后通过，未因此修改硬件逻辑。编译及打包结果见后续记录。

新版原始 Mario Clash 在经验错位模型下启动后运行 10 ms：38,856 条指令、41,042 个 ROM 响应采样、PC FFFFD7A6，所有读回比对通过，无非法指令，与版本 3 的交换副本对照相同（tests/game_v4_mario_clash.log）。

原始 Mario's Tennis 同样运行 10 ms：25,621 条指令、37,193 个 ROM 响应采样、PC FFF80144，读回全部匹配，无非法指令（tests/game_v4_mario_tennis.log）。

原始 Panic Bomber 同样运行 10 ms：38,858 条指令、41,045 个 ROM 响应采样、PC 0700002A，读回全部匹配，无非法指令，与旧理想模型基线相同（tests/game_v4_panic_bomber.log）。

原始 Wario Land 同样运行 10 ms：38,859 条指令、41,046 个 ROM 响应采样、PC FFFBE96E，读回全部匹配，无非法指令（tests/game_v4_wario_land.log）。四款覆盖 512 KiB、1 MiB、2 MiB；均为短启动检查，不宣称已模拟完整游戏或验证音视频输出。

负向对照 `-IdealReadOrder` 按预期在首次复位向量读取报错：addr 000FFFF0、got 0000、expected BC20（tests/game_v4_ideal_negative.log）。这验证严格比对没有被经验故障模式绕过；该测试预期失败，与正向启动测试区分记录。

2026-09-21 13:42 完整编译成功，用时 19 分 25 秒，0 errors、251 warnings。现有约束下最差 setup +0.075 ns、hold +0.088 ns，无负余量。资源 17,111 / 18,480 ALM（93%）、294 / 308 M10K（95%）、5 / 66 DSP。部分模板外部接口仍未完整约束，不等于整板时序认证。位流为 2,149,080 字节，RBF_R SHA-256：`8905DC548FED89E8DFE5B16978B3351286E0539160B063F9F0B1849BBFAA6FFA`。本版仍待原始 ROM 的实机复测，不将版本 3 诊断副本的成功直接标成版本 4 的硬件验证。

## 2026-09-21 实机首次故障的确定性复现

用户报告版本 3、CPU 4，故障画面为 PC `D788B23E`、IW `FA01807F`。按上轮指定的 Mario Clash 文件核对：ROM 偏移 08B23E 的首半字应为 0001，相邻后半字应为 80FE；实际 FA01 位于 08B23C，807F 位于 08B242，恰好符合每个 32 位字内半字交换（字节地址 XOR 2）。

顶层测试新增 `-SwapReadBeat` 注入读半字互换。对齐 FPGA 寄存器 RAM 的零上电条件后，模拟提交 209 条指令即在 `D788B23E` 捕获 `FA01807F`，与用户实机记录逐位一致（tests/game_swap_exact_fault.log）。此前未初始化的行为 RAM 产生 X，导致错误代码在 D788000A 无法继续；测试环境现在在上电时初始化 GPR 两份 RAM 为零，与综合分支的 power_up_uninitialized=FALSE 一致，不修改 CPU 复位逻辑。

这确定了可复现的 ROM 半字错位故障模式，但尚未区分是加载布局还是读取突发拍选择造成。当前已发布位流未作补偿。新增 make_halfword_probe.ps1 生成私有诊断副本，原文件不修改，变换可逆且逐字节验证；该 ROM 不打入发行包。需要在同一版本 3 核心上加载副本进行对照，不能将它当成核心修复。

诊断副本在同一 SwapReadBeat 模型下运行 10 ms，恢复正常复位入口和启动序列，提交 38,856 条指令、41,042 个 ROM 响应采样，无非法指令，与正常 ROM/正常读取的对照一致（tests/game_swap_compensated.log）。这只验证输入变换能够补偿该故障模式，不证明游戏完整运行。副本 SHA-256：`7BFE65B2AE611DE200F4A312234E4F6A3EE4010DDE9E7EE2B0FF55EE9DCC797F`。

## 2026-09-20 首次非法指令捕获（0.1.3-diag）

用户确认 rom/ 内各 .vb 都有相同黑屏表现。已读取该目录，并选择两款用户本地 ROM 做启动仿真；未下载或分发 ROM。

预装外部 SDRAM 模型、实际控制器/Intel DDIO 输入模型、真实顶层清零与默认复位入口：Mario Clash 启动后 10 ms 提交 38,856 条指令，41,042 个有效 ROM 响应采样；Panic Bomber 提交 38,858 条，41,045 个响应采样。所有观察到的 ROM 读回均与文件相符，未检测到非法指令，仍处于启动等待循环。响应采样包含 READY 保持周期，不等同于独立请求数。尚未证明完整游戏启动、完整文件下载或板级内存正确。

输入文件 SHA-256：Mario Clash = `9F1BAA93A805A4FC471860FB7C806BB7AC8C289DED77A78397EF8F964A11A396`；Panic Bomber = `E75E98ADD227EA9C2168FF13B131E3C72FAA789E1EF4B9FDBAE13D746AD9B3E3`。日志为 tests/game_mario_clash.log、tests/game_panic_bomber.log。

新增首次非法指令 PC/IW 锁存和测试画面两行十六进制显示；仅暴露 CPU 现有寄存器，不改变执行。八项回归通过，新增覆盖 0..F 的七段字形、两行顺序、顶层锁存和首次故障防覆盖。无故障时仍显示原棋盘格。

Mario Clash 进一步延长到启动后 50 ms：170,042 条指令、188,733 个有效 ROM 响应采样，末尾 PC = FFFFD7D8，已经退出最初等待循环并进入初始化代码；所有 ROM 比对通过，未触发非法指令。日志 tests/game_mario_clash_50ms.log。该短窗口未验证可见游戏画面或声音。

首次 0.1.3 编译于 2026-09-20 20:35 完成，但最差 setup 为 -0.409 ns（0°C 为 -0.146 ns）；打包脚本拒绝发布。超时路径从 SDRAM 的 ST_RCD/ST_COLUMN 状态及写入数据选择到 dq_out_q。2026-09-21 将移动 SDRAM 的格式化写入数据在 tRCD 阶段提前寄存到移位缓冲，最终数据引脚寄存器直接取缓冲低 16 位；写入命令、输出使能和各拍输出时刻不变。不修改时序例外，不以放宽约束掩盖超时。最终结果另记。

写数据改动后八组回归、厂商 DDIO 模型下的独立 SDRAM 测试均通过。Mario Clash 再跑 50 ms，仍为 170,042 条指令、188,733 个 ROM 响应采样、末尾 PC FFFFD7D8、无非法指令；结果与改动前一致，日志 tests/game_mario_clash_50ms_20260921.log。故障比对脚本已用复位向量测试匹配、字节交换后的不匹配，以及非 ROM 地址拒绝行为。

2026-09-21 08:24 完整编译成功（0 errors、251 warnings），所有现有约束的时序摘要无负余量：最差 setup +0.243 ns、hold +0.091 ns。资源：17,505 / 18,480 ALM（95%），294 / 308 M10K（95%），5 / 66 DSP。未修改时序例外；模板部分外部接口仍未完整约束，不能视为板级认证。0.1.3 诊断包全部 2,172,368 个 RBF_R 字节验证通过，SHA-256：`DABE62FA3CBA5152D0DDB20BF8334C5D78D7AD42F26A1134FE842320E48002E1`。尚待首次故障画面的实机反馈，不宣称黑屏修复。

## 2026-09-20 运行及输出诊断（0.1.2-diag）

后续实机反馈：ROM load status = 0、Diagnostic version = 2、CPU activity = 4、VIP activity = 2、VSU activity = 0；独立白边棋盘格和测试音均出现。基本音视频输出路径可用；游戏侧观察到非法指令历史锁存、帧同步但无非黑像素、全零音频样本。尚未取得首次非法指令地址/指令字，不能区分取指数据错误、控制流错误和 CPU 解码问题。

补充运行 `tests/sdram_vendor.do`：使用 SYNTHESIS 分支和 Intel altera_mf 实际 DDIO 仿真模型，512 次镜像读取、16 次高地址/重新加载读取全部通过（日志 tests/sdram_vendor.log）。原顶层启动测试也已使用厂商模型；这次将独立 SDRAM 测试的覆盖扩展到同一分支。外部 SDRAM 仍是简化芯片模型，不证明板上数据正确；未据此修改已发布位流。

用户实机确认上一版 ROM load status = 0、Boot stage = 10、Diagnostic version = 1。启动链路已至少到达一次指令提交，但四款游戏黑屏原因尚未确定。

新增独立的 384×224 灰阶棋盘格和低幅度左右声道测试音，并以约 210 ms 窗口监测原始游戏的 CPU 提交、VIP 帧同步/非黑像素和 VSU 非零样本。CPU HALT 为当前状态，非法指令为复位前锁存状态；非零音频样本不等于有效声音。输出测试不改变游戏运行，默认关闭。

八组回归已通过，其中新增测试验证完整帧 86,016 个有效像素、312 个行同步、单周期同步脉冲、消隐/SKIP、棋盘格颜色、测试音幅度，以及活动/静默/HALT/非法指令监测。物理音视频引脚尚待实机验证。

2026-09-20 15:14 完整编译成功（0 errors、250 warnings），现有约束的时序摘要无负余量：最差 setup +0.184 ns、hold +0.098 ns。17,196 / 18,480 ALM（93%），294 / 308 M10K（95%），5 / 66 DSP。模板未完整约束的外部接口仍是限制，不能视为整板认证。

本次八组回归再次通过，顶层另验证诊断版本 2、HALT 状态读回、只读状态写保护和输出测试开关的跨时钟传递。安装包全部 2,149,412 个 RBF_R 字节、菜单、元数据和文档一致性校验通过。位流 SHA-256：`6A808F52208BFEF9D02E2B4061DD50A0FD7DFC65CC17FC21681F1575CBDB277A`。

## 2026-09-17 用户实机反馈：启动失败

Pocket 掌机本体、固件 2.7：Wario Land（2 MB）、Mario Clash（1 MB）、Mario Tennis（512 KB）、Panic Bomber（512 KB）均黑屏、无声音，核心菜单可以打开。旧菜单 ROM transfer error 置灰且看不到数值，尚不能确定 FIFO 故障位。

因此下列编译/仿真通过结果不代表游戏可以运行，2026-09-16 候选包实机启动验证失败。当前提供仅修改 interact.json 的文字诊断菜单覆盖包，用已有位流读取 0x20000008；该包不是黑屏修复。读到 0 也只排除溢出锁存，不能证明下载、CPU 或 SDRAM 正常。

用户安装菜单覆盖后反馈 `0 - No FIFO overflow`，数值项仍不可读。进一步发现旧顶层返回值随当前桥地址变化，没有保持到下一次读取脉冲。新加的保持测试在旧代码上确实失败，修复后通过，因此旧菜单的 0 不足以证明实际启动链路正常。

0.1.1-diag 增加文字 Boot stage 和 Diagnostic version。七项回归于 2026-09-17 12:22 全部通过：新增测试串联真实 core_top、APF 命令处理、Intel 双时钟 FIFO 模型、SDRAM 控制器、完整 RAM 清零以及默认 FFFFFFF0 复位向量，原创程序经 ROM 镜像取指后向卡带 SRAM 写入 0x34。约 18.024 ms 后复位释放，启动阶段达到 10。该测试使用理想 PLL 时钟与简化外部内存模型；不验证物理 SPI、板级时序或商业游戏。

## 2026-09-17 诊断包 0.1.1-diag

12:37 完整编译与现有约束时序摘要检查通过。16,923 / 18,480 ALM（92%），294 / 308 M10K（95%），5 / 66 DSP；该结果仍不包括模板未完整约束的外部接口验证。诊断位流为 2,145,836 字节，逐字节位反转验证通过。该版本尚待用户实机反馈，不宣称已修复四款游戏黑屏。

最差 setup 余量 +0.013 ns，最差 hold +0.105 ns。setup 裕量极小，不能据此推断板上 SDRAM 一定可靠。诊断位流 SHA-256：`20E166C7C1A68CBA739365440EE6090FD0F873F66991B930FBC944F32479468C`。

## 2026-09-16 候选包（已报告实机黑屏）

15:09 完整编译与现有约束下的全部时序摘要检查通过，已生成 release。资源为 16,987 / 18,480 ALM（92%）、294 / 308 M10K（95%）、5 / 66 DSP。四个速度/温度模型的 setup、hold、recovery、removal、最小脉宽摘要均无负余量；最差 setup +0.155 ns，最差 hold +0.110 ns。余量较小，不应推断为可提高频率。

六项回归全部通过；安装包配置与源文件一致，全部 2,148,660 个 RBF_R 字节已独立验证位反转。位流 SHA-256：`57D5CA611A6103A9DE26F3A012B9C545A24CEDCD5AD84955722ED6CF7C3B8847`。

这是待上机验证的实验包，不是完整板级时序认证：时钟均有约束，但仍有 4 个输入端口、23 个输出端口缺少完整 I/O 时序约束（模板的 SPI/PAD/音视频接口）。不能将摘要通过理解为这些接口也已验证。

## 已通过

- Quartus Prime Lite 25.1std 分析与综合：成功。去掉 MiSTer 三帧显示缓存后，块 RAM 为 2,792,960 bit；14,988 个寄存器。
- Questa 2025.2 仿真：
  - ROM 队列背压、最终写入排空、重新加载；卡带 SRAM 字节写入/地址映射；手柄映射；49 个声道时段的 I2S 数据。
  - 一行 384 个有效像素；单周期 HS/VS；SKIP 和消隐 RGB 元数据。
  - 移动 SDRAM 初始化（含扩展模式寄存器）、32 位写入、512 次带标签/镜像读取，以及重新加载后 16 次稀疏地址读取（跨行和 16 MiB 范围高地址）；在引脚上检查 72 ns 行周期、120 ns 刷新恢复、21 ns RCD 和 35 ns 写关闭间隔。简化引脚模型下写入约 4.05 MB/s，不代表完整 16 MiB 镜像下载吞吐量。
  - 外部 WRAM 的 55 ns 读访问模型、75 ns 写脉冲、字节使能、连续清零请求和事务标签。
  - 整数/浮点乘法的 DSP 表达式与上游原始模块逐周期对照：1024 组输入，比较结果、标志、异常、忙状态及完成周期。tests/reference_* 仅重命名了上游模块用于此对照，保留原版权。
  - 原创小型 V810 程序通过真实 CPU/VUE/VIP/VSU 层次运行，完成外部 WRAM 字/字节读写及卡带 SRAM 写入后 HALT（534 个系统周期）。ROM 使用理想读模型，外部 WRAM 使用 55 ns 读模型；此测试不验证商业游戏或实际 SDRAM 下载链路。

首次布局失败：三帧缓存导致所需 M10K 超过芯片 308 个。已切换到原 RTL 的直接 VRAM 扫描路径，不能把首次失败当作最终版本验证结果。

第二次布局仍失败：实际 RAM 分块超过 308，逻辑为 19,027 / 18,480 ALM。后续修改将 64 KiB WRAM 移至外部 SRAM，并启用面积优化；上述综合数字是修改前的历史结果，不是最新版本的资源承诺。

工作内存外移后一次综合得到 2,268,672 bit 块 RAM。随后又加入 DSP 乘法优化并重新启动完整编译，最终数字以最新报告为准。

2026-09-15 15:37 的完整编译成功生成位流：17,375 / 18,480 ALM（94%），294 / 308 M10K（95%），5 / 66 DSP。但最差 setup slack 为 -0.869 ns，不能视为时序通过。已定位为 SDRAM 控制器内部路径，随后针对该模块单独启用速度优化并重新编译。

2026-09-16 09:36 的完整编译仍为 -0.869 ns，单独启用速度优化未解决问题。随后将移动 SDRAM 行关闭比较结果寄存，增加引脚命令间隔检查后六项回归全部通过。

14:38 完整编译：17,142 / 18,480 ALM（93%）、294 M10K、5 DSP，最差 setup slack 为 -0.660 ns（85°C），0°C 为 -0.545 ns。打包脚本按预期拒绝生成 release。详细路径转为请求选择/等待计数到地址、数据引脚。

随后在移动 SDRAM 分支增加独立 ACTIVATE 与 COLUMN 发命令阶段，避免请求仲裁和宽等待计数直接控制引脚。14:53 六项回归全部通过（含新增高地址/重新加载测试），14:51 启动的第二轮完整编译于 15:09 通过，结果见本文件顶部。

模板自带 APF 的 SPI/PAD 和音视频引脚没有完整板级 I/O 约束。必须保留这一限制，不能用宽泛 false-path 把未验证接口伪装成时序通过；内部/内存时序与整板时序是不同结论。接口关系参考 [Analogue 官方说明](https://www.analogue.co/developer/docs/bus-communication)。

## 待完成

- 补齐板级 I/O 时序依据并完成约束审计；本轮布局布线和现有约束检查已通过，日志为根目录 build.log，报告位于 src/fpga/output_files。
- 实际 Pocket ROM 加载吞吐量和队列是否溢出。
- 多款游戏启动、画面稳定性、双眼同步、音频噪声、菜单往返、软重置与更换 ROM。
- SRAM 保存、退出、断电重启恢复，及与其他模拟器的 8 KiB 存档互通。
- Dock 右摇杆/L2/R2、HDMI 输出及长期运行。

没有连接 Pocket 真机；截至 2026-09-20 已使用用户本地商业 ROM 做上述短期启动仿真，但未验证完整游戏运行。SDRAM 仿真不是 Alliance 官方全功能器件模型。SDRAM I/O 延时约束仍需结合器件时序和板级走线审查；通过部分接口仿真不表示板级时序正确。

## 主要适配边界

- core_top.v 不再依赖 MiSTer HPS、DDR3、SD 卡或 Linux 服务。
- VIP RAM 保留；WRAM 使用 Pocket 外部异步 SRAM，ROM 使用移动 SDRAM；卡带存档使用双时钟片上 RAM。
- 外部 WRAM READY 门控可能增加 CPU 等待时间；游戏速度和周期精确性需要继续验证和优化。
- vb_cart_sdram 的后台 SRAM 接口在本工程禁用；其 32 位加载口修改不应直接反向移植到 MiSTer。
- 保存快照、休眠、作弊及联机均明确禁用。
- 模板遗留 output_files 位流不属于本核心，只有本次成功编译产生的新文件才可打包。
