# Pocket 首轮上机检查

只使用当前源码成功构建并通过时序摘要检查的 release 包。此清单尚未执行；仿真通过不代表下面各项已通过。

## 准备

1. 备份 SD 卡和已有存档，使用单独的测试卡更稳妥。
2. 将 release 内 Cores、Platforms、Assets 合并到卡根目录，不要复制模板位流。
3. 在 Assets/virtualboy/common 放入合法取得的 ROM。不随本工程提供游戏。
4. 记录 Pocket 固件版本、是否使用 Dock、ROM 文件名/大小/校验值和测试包日期。

## 加载与基本运行

1. 先使用体积较小、启动行为已知的 ROM；记录从选择文件到首帧的时间。
2. 对 0.1.5 诊断版，先确认 Diagnostic version 为 5，使用原始 ROM（禁止使用 halfword probe 副本），Output test 为 Off。菜单期间 CPU activity=5 为预期暂停状态；非法指令历史仍优先显示 4。按 DIAGNOSTIC.md 检查菜单停留 10 秒后恢复、静音、按键释放保护与快速开关。出现 CPU 4 时打开 Output test，关闭菜单并拍下首次故障数字。旧版置灰数值项不可用，且旧读回逻辑有保持缺陷，不能用它断定无故障。
3. 检查左右眼单独显示、红青和白色模式，观察顶部/底部是否缺行、颜色是否异常、画面是否抖动或撕裂。
4. 分别测试 Standard、Dual-pad 布局。Pocket 本体无法独立提供所有 VB 按键；完整按键需 Dock 手柄。
5. 检查左右声道和音高，运行至少 10 分钟，观察持续噪声、爆音、画面或速度变化。
6. 返回菜单后继续、软重置、换 ROM，并测试较大 ROM，确认没有沿用旧游戏数据或队列溢出。

## 存档与 Dock

1. 在支持存档的游戏中创建一个容易辨认的新进度，然后正常退出。
2. 确认存档大小为 8192 字节；重新加载、正常关机再开机，检查进度是否恢复。不要在写入期间断电。
3. 备份这个存档，再测试与其他模拟器的存档互通；不要覆盖唯一副本。
4. Dock 单独检查 HDMI 画面、音频、右摇杆四向和 L2/R2。

## 报告问题

保留 ROM 校验值（无需上传 ROM）、复现步骤、画面照片/短视频、队列故障值、核心和固件版本。特别标记是首次启动、换 ROM 后还是存档恢复后发生。

目前重点风险：外部 WRAM 等待造成速度差异、直接 VRAM 扫描产生撕裂/双眼不同步、文件桥突发吞吐、分数音频时钟的实机音质，以及模板未完整约束的 APF 外部接口。
