# 移植边界与维护说明

输入来自用户提供的相邻目录 VirtualBoy_MiSTer 和 core-template-1.3.0；两个目录均不含可查询的 Git 提交信息。本项目没有修改输入目录。

发行元数据作者为 `maxhoov`，核心目录为 `Cores/maxhoov.VirtualBoy`；平台分类固定 `Console`。打包显式包含 `Platforms/_images/virtualboy.bin`，测试检查图片尺寸、零低字节、与源文件的哈希一致及不是模板示例。`tools/build-platform-image.cjs` 从原创 SVG 生成图片并做无损编解码回读检查。作者更名和平台图不修改 FPGA 位流，Diagnostic version 仍为 4。

## 平台外壳

`src/fpga/core/core_top.v` 连接 APF、时钟、ROM 队列、内存、按键及音视频。APF 协议实现及 Pocket 引脚分配沿用模板。MiSTer HPS、DDR3、OSD、SD 卡服务不参与构建。

桥总线使用大端模式；文件数据先转换为 Virtual Boy 的小端字节顺序。版本 4 额外对 ROM 的每个 32 位字交换两个半字，补偿实机观察到的地址 XOR 2；SRAM 不做此补偿。菜单寄存器是数值，不进行文件字节转换。

| 桥地址 | 用途 |
|---|---|
| 00000000–00FFFFFF | 数据槽 0，ROM 写入，最多 16 MiB |
| 10000000–10001FFF | 数据槽 1，8 KiB 卡带存档 |
| 20000000 | 显示模式，0 左红 / 1 右红 / 2 红青 / 3 左白 |
| 20000004 | 按键布局，0 Standard / 1 Dual-pad |
| 20000008 | ROM 队列故障，bit 0 为溢出锁存 |
| 2000000c | 只读启动阶段 0..11 |
| 20000010 | 只读诊断版本，本版 5 |
| 20000014 | 只读 CPU 活动 0..5，5 为菜单已暂停，其余为原活动窗口状态 |
| 20000018 | 独立输出测试，0 游戏 / 1 棋盘格与测试音 |
| 2000001c | 只读 VIP 活动 0..3 |
| 20000020 | 只读 VSU 非零样本活动 0..1 |
| F8xxxxxx | APF 命令，保留区域 |

首次非法指令 PC/IW 在 40 MHz 域锁存，仅在 Output test 画面显示，不经桥总线读回。CPU 新增输出只暴露现有执行寄存器，不修改指令执行。

0.1.5 将 APF 00B0 菜单通知跨两级寄存器同步到 clk_sys，接入已有 CPU/VIP 排空暂停。Pocket 使用 virtualboy 参数 PAUSE_KEEP_SCANOUT=1，停止 core CE 后保留独立 video CE，不触发存档专用扫描边界停止；默认参数仍为 0，其他调用的原存档行为不变。该参数仅供菜单冻结，不可将其 pause_ready 当成完整存档检查点。音频菜单期间发送零样本但保持 I2S/时钟，按钮屏蔽到菜单关闭且全部映射按键释放。CPU activity=5 表示已暂停，非法指令历史 4 优先。

`tests/run_game.ps1 -RomPath 'rom/<文件名>.vb' -MenuPause` 在启动检查后执行两次 APF 菜单开关，检查 CPU 冻结/恢复、连续扫描、静音/恢复和按键释放保护。保持的 25 ms 超过一帧；另一次为短暂停。测试原始游戏画面是否按预期冻结仍需掌机检查。

`tests/run_game.ps1 -RomPath 'rom/<文件名>.vb'` 使用用户本地 ROM 跑短期诊断。ROM 预装到外部 SDRAM 芯片模型，仅最后一个字经过下载链路以设置镜像掩码，因此不验证全文件下载；每次运行时 ROM 响应都会与文件比对，非法指令会停止仿真。文件内容不会写入发行包。

`tests/compare_fault.ps1 -RomPath 'rom/<文件名>.vb' -PC <8位十六进制> -Instruction <8位十六进制>` 将实机故障画面映射回 ROM，比较指令首半字。非 ROM 区域的 PC 会被拒绝，避免把 RAM/错误跳转误解为 ROM 数据损坏。后半字可能不属于当前指令，仅作为相邻数据参考。

版本 3 的 `run_game.ps1 -SwapReadBeat` 故障注入曾在 GPR 零上电模型下逐位重现实机 D788B23E/FA01807F；交换 ROM 副本后，用户确认有画面/声音、CPU 2。版本 4 在 ROM FIFO 入口交换 file_word 两半字，存档路径不变，必须使用原始 ROM。

版本 4 的顶层/游戏仿真默认建模实机观察到的读地址 XOR 2；这是经验故障模型，不是已确认的器件机制。预加载内存包含加载器变换，每个读回仍严格比对原始 ROM。`run_game.ps1 -IdealReadOrder` 移除经验故障，作为应报 ROM mismatch 的负向对照。独立 SDRAM 测试继续使用理想读顺序。`make_halfword_probe.ps1` 仅保留用于版本 3 历史复现，不能给版本 4 生成运行用 ROM，绝不将诊断 ROM 放入发行包。

## 时钟与跨域

- 74.25 MHz：APF、按键采样、文件桥、音频输出。
- 约 40 MHz：控制台、工作内存适配器、视频；每两周期一个 20 MHz 使能。
- 40 MHz、90 度：APF 转换后的源同步视频时钟。
- 约 120 MHz：移动 SDRAM 控制器。

ROM 使用双时钟 FIFO。下载完成必须同时满足桥完成通知、FIFO 排空和最后一次内存写入完成。FIFO 无法向 APF 施加背压，溢出会锁存错误并禁止启动，必须验证硬件上的实际加载吞吐量。

音频通过请求/确认传递成对左右声道样本，分数分频产生平均 12.288 MHz MCLK。它不是无抖动的专用音频 PLL，实际音频质量仍需上机检查。

## 内存适配

- VIP VRAM/DRAM 保持片上存储。
- 64 KiB WRAM 改为外部异步 SRAM。读等待及写入的建立、脉宽、保持由 `pocket_wram.sv` 控制；READY 会延长 CPU 访问，可能影响原机周期精确性。
- 卡带 SRAM 为片上双时钟 8 KiB RAM，低字节映射与上游保持一致；与工作内存是不同的存储器。
- ROM 使用移动 SDRAM，增加 EMR 初始化、32 ms 刷新周期对应的刷新间隔，并把下载写入改成 32 位。
- 移动 SDRAM 的行关闭条件提前寄存，切断年龄计数比较器到引脚命令的组合路径；额外等待一个周期，不缩短内存恢复时间。非移动 SDRAM 分支保持原行为。
- 移动 SDRAM 请求仲裁、ACTIVATE 发出、RCD 等待及 COLUMN 发出分阶段执行，减少引脚前的组合逻辑；额外延迟计入读关闭目标。非移动分支不经过这两个新增发命令阶段。
- `vb_cart_sdram` 中原有后台 SRAM 接口在本工程禁用；不要将修改后的模块直接替换回 MiSTer 而不审查该接口。
- 禁用 MiSTer 三帧展示缓存，使用原 RTL 的直接 VRAM 扫描分支。画面撕裂及双眼时间一致性仍需实机验证。

## 其他上游改动

整数及浮点乘法的 radix-16 数位乘积改用 DSP 表达式，保留原指令周期安排；测试与未修改的参考运算模块逐周期比较。V810 缓存输出声明提前，适配严格的仿真器。VIP 查表文件路径改为本工程路径。

## 构建与测试

`build.ps1` 完整编译后检查结果并打包。`build.ps1 -PackageOnly` 复用已有结果，但仍拒绝失败的布局、负时序余量或早于源文件的位流。每个 RBF 字节进行位反转后才写入 RBF_R。

`tests/run.ps1` 运行八组测试，含独立输出源和窗口活动监测；`tests/boot.do` 可单独运行原创 V810 启动程序，`tests/top_boot.do` 覆盖顶层命令、FIFO、SDRAM、清零、默认复位向量和桥接读回保持。没有商业 ROM。`src/fpga/timing_detail.tcl` 用于已布局设计的关键路径和未约束路径诊断。

软件测试、内部静态时序、外部板级时序和真机游戏验证是四个不同层次，状态以 `VALIDATION.md` 和最新编译报告为准。
